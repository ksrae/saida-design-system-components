import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as EventStories from './sy-input-clear.stories';

<Meta of={EventStories} />

# clear

Fires when the clear button is pressed (requires `clearable`).

<Canvas of={EventStories.Default} />
