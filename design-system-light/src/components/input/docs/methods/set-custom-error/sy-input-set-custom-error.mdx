import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as MethodStories from './sy-input-set-custom-error.stories';

<Meta of={MethodStories} />

# setCustomError / clearCustomError

Mark the input invalid with a custom error, and clear it later.

<Canvas of={MethodStories.Default} />
