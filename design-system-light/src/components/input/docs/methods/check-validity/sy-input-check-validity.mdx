import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as MethodStories from './sy-input-check-validity.stories';

<Meta of={MethodStories} />

# checkValidity

Returns the current validity state without showing a native popup.

<Canvas of={MethodStories.Default} />
