import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-input-autofocus.stories';

<Meta of={AttrStories} />

# Autofocus

Automatically focuses the input when the page loads or the component is rendered.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
