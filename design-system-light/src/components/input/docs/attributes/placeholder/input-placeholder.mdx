import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as InputPlaceholderStories from './input-placeholder.stories';

<Meta of={InputPlaceholderStories} />

# Placeholer
## Definition
It allows designers to provide temporary instructional text within the input field.<br/>
It offers guidance or context to users about the type of information expected in the field before they enter their own input.

## Example

<Canvas of={InputPlaceholderStories.Param} />

<Controls of={InputPlaceholderStories.Param} />
