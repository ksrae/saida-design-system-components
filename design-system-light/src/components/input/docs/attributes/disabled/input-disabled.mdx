import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as InputDisabledStories from './input-disabled.stories';

<Meta of={InputDisabledStories} />

# Disabled
## Definition
The input is disabled and cannot be interacted with.<br/>
It appears visually grayed out or with a distinct styling to indicate its disabled state.


## Boolean Attribute Handling
In the source code, disabled = "" should be modified as follows
- Set `disabled` attribute and don't put a value after it.
```
<sy-input disabled></sy-input>
```

- Alternatively, you can explicitly set it  to `true`, but to not apply the `disabled`, remove the attribute instead of setting its value to`false`.
```
<sy-input disabled="true"></sy-input>
```

## Example
<Canvas of={InputDisabledStories.Param} />

<Controls of={InputDisabledStories.Param} />
