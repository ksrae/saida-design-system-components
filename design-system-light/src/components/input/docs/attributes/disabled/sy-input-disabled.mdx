import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-input-disabled.stories';

<Meta of={AttrStories} />

# Disabled

Disables user interaction and visually grays out the input.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
