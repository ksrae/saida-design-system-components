import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-input-required.stories';

<Meta of={AttrStories} />

# Required

Marks the field required for form validation.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
