import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-input-name.stories';

<Meta of={AttrStories} />

# Name

`name` attribute used when the input is submitted inside a form.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
