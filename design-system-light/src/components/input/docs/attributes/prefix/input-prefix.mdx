import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as InputPrefixStories from './input-prefix.stories';

<Meta of={InputPrefixStories} />

# Prefix
## Definition
To set the prefix icon with slot, add `slot="prefix"` attribute to the tag. <br/>
This is useful to add prefix icon on the input field.


## Example
<Canvas of={InputPrefixStories.Param} />

