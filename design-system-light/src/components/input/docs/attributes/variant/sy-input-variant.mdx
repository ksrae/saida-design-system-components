import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-input-variant.stories';

<Meta of={AttrStories} />

# Variant

Legacy alias for `type`. Accepts `text`, `password`, or `search`.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
