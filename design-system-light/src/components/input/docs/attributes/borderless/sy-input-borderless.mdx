import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-input-borderless.stories';

<Meta of={AttrStories} />

# Borderless

Removes the visible border around the input for a minimal look.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
