import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-input-type.stories';

<Meta of={AttrStories} />

# Type

Native input type — `text`, `password`, `email`, `number`, `tel`, `url`, `search`.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
