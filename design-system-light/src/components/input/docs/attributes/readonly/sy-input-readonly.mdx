import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-input-readonly.stories';

<Meta of={AttrStories} />

# Readonly

Makes the input non-editable while preserving its appearance.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
