import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as SlotStories from './sy-input-suffix.stories';

<Meta of={SlotStories} />

# Suffix Slot

Content projected via `slot="suffix"` appears after the input text.

<Canvas of={SlotStories.Default} />
