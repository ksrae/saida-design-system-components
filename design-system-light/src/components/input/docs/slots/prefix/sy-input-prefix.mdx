import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as SlotStories from './sy-input-prefix.stories';

<Meta of={SlotStories} />

# Prefix Slot

Content projected via `slot="prefix"` appears before the input text.

<Canvas of={SlotStories.Default} />
