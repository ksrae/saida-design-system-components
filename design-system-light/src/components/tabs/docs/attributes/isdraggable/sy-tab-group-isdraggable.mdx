import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-tab-group-isdraggable.stories';

<Meta of={AttrStories} />

# Draggable

The `draggable` attribute. When `true`, tabs can be reordered by drag-and-drop.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
