import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as TabGroupDisabledStories from './tab-group-disabled.stories';

<Meta of={TabGroupDisabledStories} />

# Group Disabled
This attribute to disable all tabs at once.<br/>
The tab content of the last active tab is exposed.<br/><br/>
The default value is false.


## Boolean Attribute Handling
In the source code, <b>disabled = ""</b> should be modified as follows
- Set `disabled` attribute and don't put a value after it.
```
<sy-tab-group disabled></sy-tab-group>
```
- Alternatively, you can explicitly set it  to true, but to not apply the `disabled`, remove the attribute instead of setting its value to false.
```
<sy-tab-group disabled="true"></sy-tab-group>
```


## Example
<Canvas of={TabGroupDisabledStories.Param} />

<Controls of={TabGroupDisabledStories.Param} />

