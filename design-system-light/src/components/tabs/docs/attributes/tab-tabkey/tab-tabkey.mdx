import { Canvas, Meta, Controls, Story } from '@storybook/blocks';

import * as TabKeyStories from './tab-tabkey.stories';

<Meta of={TabKeyStories} />

# Tabkey
## Definition
This is required to connect the tab and its content, and is also required when the tab is closed.<br/>
When a tab is selected, the Tab Content is exposed which has the name property matching the tab's key.<br/><br/>
This property must be set to each tab.


## Example
<Canvas of={TabKeyStories.Param} />

<Controls of={TabKeyStories.Param} />

