import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-tab-current-disabled-status.stories';

<Meta of={AttrStories} />

# Current Disabled Status

The `currentDisabledStatus` attribute.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />