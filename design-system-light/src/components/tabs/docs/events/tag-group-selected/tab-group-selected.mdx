import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as TabGroupSelectedStories from './tab-group-selected.stories';

<Meta of={TabGroupSelectedStories} />

# Selected
## Definition
The selected event is fired when a tab is selected. <br/>
When a tab is selected, the tab content with the same name as the tab's tabkey becomes active. <br/>
A inactive(or disabled) tab cannot be selected.


## Example
<Canvas of={TabGroupSelectedStories.Param} />

