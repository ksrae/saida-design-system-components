import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as TabGroupOrderedStories from './tab-group-ordered.stories';

<Meta of={TabGroupOrderedStories} />

# Ordered
## Definition
If tabs are repositioned via drag, the ordered event is fired. <br/>
The ordered event contains the key and index values of all tabs in e.detail in order.


## Example
<Canvas of={TabGroupOrderedStories.Param} />

