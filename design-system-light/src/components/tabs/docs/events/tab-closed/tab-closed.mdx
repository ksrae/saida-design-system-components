import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as TabClosedStories from './tab-closed.stories';

<Meta of={TabClosedStories} />

# Closed
## Definition
Clsed event is fired when the tab's close icon is clicked.<br/>
To expose a tab's close icon, each tab must have the closable property.


## Example
<Canvas of={TabClosedStories.Param} />


