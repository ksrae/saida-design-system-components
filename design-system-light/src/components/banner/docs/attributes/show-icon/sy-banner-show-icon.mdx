import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as ShowIconStories from './sy-banner-show-icon.stories';

<Meta of={ShowIconStories} />

# Show Icon

The `showIcon` attribute toggles the variant icon (info/success/warning/error) beside the message.

<Canvas of={ShowIconStories.Default} />
<Controls of={ShowIconStories.Default} />
