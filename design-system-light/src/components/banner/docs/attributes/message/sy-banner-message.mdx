import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as MessageStories from './sy-banner-message.stories';

<Meta of={MessageStories} />

# Message

The `message` attribute is the main body text of the banner.

<Canvas of={MessageStories.Default} />
<Controls of={MessageStories.Default} />
