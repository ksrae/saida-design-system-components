import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as BannerneutralIconStories from './banner-neutralIcon.stories';


<Meta of={BannerneutralIconStories} />

# NeutralIcon
## Definition
If the banner message variant is <b>neutral</b> and <b>icon</b> property is set to true, the `neutralIcon` can be applicable.<br/>
It should be one of the types available in the <b>sy-icon</b> component.

## Example
<Canvas of={BannerneutralIconStories.Param} />

<Controls of={BannerneutralIconStories.Param} />

