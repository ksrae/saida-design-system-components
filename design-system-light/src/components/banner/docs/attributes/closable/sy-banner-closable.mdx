import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as ClosableStories from './sy-banner-closable.stories';

<Meta of={ClosableStories} />

# Closable

The `closable` attribute adds a close button to dismiss the banner.

<Canvas of={ClosableStories.Default} />
<Controls of={ClosableStories.Default} />
