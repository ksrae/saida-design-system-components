import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as HeaderStories from './sy-banner-header.stories';

<Meta of={HeaderStories} />

# Header

The `header` attribute is the bold title shown above the message.

<Canvas of={HeaderStories.Default} />
<Controls of={HeaderStories.Default} />
