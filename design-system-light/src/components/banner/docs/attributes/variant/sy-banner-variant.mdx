import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as VariantStories from './sy-banner-variant.stories';

<Meta of={VariantStories} />

# Variant

The `variant` attribute sets the banner color/severity.
Options: `info` (default), `success`, `warning`, `error`, `neutral`.

<Canvas of={VariantStories.Default} />
<Controls of={VariantStories.Default} />
