import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as VerticalStories from './sy-button-group-vertical.stories';

<Meta of={VerticalStories} />

# Vertical

The `vertical` attribute stacks buttons vertically instead of horizontally.

<Canvas of={VerticalStories.Default} />
<Controls of={VerticalStories.Default} />
