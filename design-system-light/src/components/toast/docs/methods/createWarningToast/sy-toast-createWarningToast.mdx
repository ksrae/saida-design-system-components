import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as MethodStories from './sy-toast-createWarningToast.stories';

<Meta of={MethodStories} />

# CreateWarningToast

The `createWarningToast` method.

<Canvas of={MethodStories.Default} />