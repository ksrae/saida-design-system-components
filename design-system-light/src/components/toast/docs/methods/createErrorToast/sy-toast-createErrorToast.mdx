import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as MethodStories from './sy-toast-createErrorToast.stories';

<Meta of={MethodStories} />

# CreateErrorToast

The `createErrorToast` method.

<Canvas of={MethodStories.Default} />