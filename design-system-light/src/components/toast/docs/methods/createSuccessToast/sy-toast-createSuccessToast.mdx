import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as MethodStories from './sy-toast-createSuccessToast.stories';

<Meta of={MethodStories} />

# CreateSuccessToast

The `createSuccessToast` method.

<Canvas of={MethodStories.Default} />