import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as MethodStories from './sy-toast-createNeutralToast.stories';

<Meta of={MethodStories} />

# CreateNeutralToast

The `createNeutralToast` method.

<Canvas of={MethodStories.Default} />