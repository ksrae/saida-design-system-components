import { Canvas, Meta, Controls, Story } from "@storybook/blocks";

import * as AvatarParamStory from "./avatar.stories";
import * as AvatarGroupParamStory from "./avatar-group.stories";

<Meta of={AvatarParamStory} />

# Avatar
## Definition
Avatar components are intended to represent a person or an object. By default, the two characters from the user's name will be shown.<br/>
You can personalize avatars by adding custom icons, initials, and images.

## Example
<Canvas of={AvatarParamStory.Default} />

<Controls of={AvatarParamStory.Default} />


# Avatar Group
## Definition
Avatar Group renders its children as a stack by default.
Use the maxCount prop to limit the number of avatars.

## Example
<Canvas of={AvatarGroupParamStory.Default} />

<Controls of={AvatarGroupParamStory.Default} />
