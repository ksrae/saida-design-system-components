import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as SizeStories from './sy-avatar-size.stories';

<Meta of={SizeStories} />

# Size

The `size` attribute controls the avatar size. Values: `small`, `medium` (default), `large`.

<Canvas of={SizeStories.Default} />
<Controls of={SizeStories.Default} />
