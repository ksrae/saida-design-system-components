import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as AvatarDisabledStories from './avatar-disabled.stories';


<Meta of={AvatarDisabledStories} />

# Disabled
## Definition
Use the disabled attribute to disable a avatar. <br/>
If disabled attribute is set, any events will be prevented.

<br/>

## Boolean Attribute Handling
In the source code, disabled = "" should be modified as follows
- Set "disabled" attribute and don't put a value after it.
```
<sy-avatar disabled></sy-avatar>
```
- Alternatively, you can explicitly set it  to `true`, but to not apply the `disabled`, remove the attribute instead of setting its value to`false`.

```
<sy-avatar disabled="true"></sy-avatar>
```

## Example
<Canvas of={AvatarDisabledStories.Param} />

<Controls of={AvatarDisabledStories.Param} />