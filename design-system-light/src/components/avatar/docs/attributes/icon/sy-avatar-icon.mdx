import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as IconStories from './sy-avatar-icon.stories';

<Meta of={IconStories} />

# Icon

The `icon` attribute renders an SVG icon inside the avatar.

<Canvas of={IconStories.Default} />
<Controls of={IconStories.Default} />
