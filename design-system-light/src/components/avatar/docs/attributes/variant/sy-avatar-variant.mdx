import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as VariantStories from './sy-avatar-variant.stories';

<Meta of={VariantStories} />

# Variant

The `variant` attribute controls the avatar background color.
Options: `lightgray`, `red`, `orange`, `yellow`, `lime`, `green`, `teal`, `blue`, `purple`, `magenta`, `darkgray`.

<Canvas of={VariantStories.Default} />
<Controls of={VariantStories.Default} />
