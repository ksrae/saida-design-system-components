import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as TooltipContentStories from './sy-avatar-tooltip-content.stories';

<Meta of={TooltipContentStories} />

# Tooltip Content

The `tooltipContent` attribute (alias `tooltip-content`) shows a native tooltip when hovering over the avatar.

<Canvas of={TooltipContentStories.Default} />
<Controls of={TooltipContentStories.Default} />
