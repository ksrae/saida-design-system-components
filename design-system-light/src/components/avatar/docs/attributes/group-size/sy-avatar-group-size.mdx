import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as SizeStories from './sy-avatar-group-size.stories';

<Meta of={SizeStories} />

# Size (Group)

The `size` attribute sets the size of every avatar rendered inside the group.

<Canvas of={SizeStories.Default} />
<Controls of={SizeStories.Default} />
