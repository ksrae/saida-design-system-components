import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-tree-search-term.stories';

<Meta of={AttrStories} />

# Search Term

The `searchTerm` attribute.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />