import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-tree-item-append-placeholder.stories';

<Meta of={AttrStories} />

# Append Placeholder

The `appendPlaceholder` attribute.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />