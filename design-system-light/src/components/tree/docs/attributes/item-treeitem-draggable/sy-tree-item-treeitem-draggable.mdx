import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-tree-item-treeitem-draggable.stories';

<Meta of={AttrStories} />

# Treeitem Draggable

The `treeitemDraggable` attribute.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />