import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-tree-item-selected-value.stories';

<Meta of={AttrStories} />

# Selected Value

The `selectedValue` attribute.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />