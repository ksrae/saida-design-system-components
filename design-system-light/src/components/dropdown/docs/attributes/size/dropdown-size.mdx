import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as DropdownSizeStories from './dropdown-size.stories';

<Meta of={DropdownSizeStories} />

# Size
## Definition
Sets the size of the dropdown. <br/>
There are three types: `small`, `medium`, and `large`<br/>
The default value is `medium`.<br/>

## Example
<Canvas of={DropdownSizeStories.Param} />

<Controls of={DropdownSizeStories.Param} />
