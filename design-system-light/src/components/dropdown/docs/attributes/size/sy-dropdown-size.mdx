import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as SizeStories from './sy-dropdown-size.stories';

<Meta of={SizeStories} />

# Size

The `size` attribute adjusts trigger height and menu padding: `small`, `medium`, `large`.

<Canvas of={SizeStories.Default} />
<Controls of={SizeStories.Default} />
