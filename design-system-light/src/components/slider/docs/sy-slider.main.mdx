import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as SliderStories from './sy-slider.stories';

<Meta of={SliderStories} />

# Slider

## Definition
A horizontal/vertical range input with tooltip, marks, snap and range selection support.

## Example
<Canvas of={SliderStories.Default} />
<Controls of={SliderStories.Default} />
