import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as SliderReadonly from './slider-readonly.stories';

<Meta of={SliderReadonly} />

# Readonly
## Definition
The readonly state of the slider. When set to true, the slider cannot be changed by the user.

## Boolean Attribute Handling
In the source code, <b>readonly = ""</b> should be modified as follows
- Set `readonly` attribute and don't put a value after it.
```
<sy-slider readonly></sy-slider>
```
- Alternatively, you can explicitly set it to true, but to not apply `readonly`, remove the attribute instead of setting its value to false.

```
<sy-slider readonly="true"></sy-slider>
```

## Example
<Canvas of={SliderReadonly.Param} />

<Controls of={SliderReadonly.Param} />

