import { Canvas, Meta, Controls, Story } from "@storybook/blocks";

import * as BadgeParamStory from "./badge.stories";

<Meta of={BadgeParamStory} />

# Badge
## Definition
A badge is a small, attention-grabbing visual indicator used to display statues, counts, or labels in a compact format. 

## Example
<Canvas of={BadgeParamStory.Default} />

<Controls of={BadgeParamStory.Default} />


