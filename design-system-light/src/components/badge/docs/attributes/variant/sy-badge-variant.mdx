import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as VariantStories from './sy-badge-variant.stories';

<Meta of={VariantStories} />

# Variant

The `variant` attribute sets the badge color.
Options: `red` (default), `yellow`, `green`, `blue`, `gray`.

<Canvas of={VariantStories.Default} />
<Controls of={VariantStories.Default} />
