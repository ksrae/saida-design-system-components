import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as ValueStories from './sy-badge-value.stories';

<Meta of={ValueStories} />

# Value

The `value` attribute is the numeric content shown inside the badge.

<Canvas of={ValueStories.Default} />
<Controls of={ValueStories.Default} />
