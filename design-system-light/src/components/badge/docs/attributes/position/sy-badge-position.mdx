import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as PositionStories from './sy-badge-position.stories';

<Meta of={PositionStories} />

# Position

The `position` attribute determines where the badge is placed relative to the slotted content.
Values: `topLeft`, `topRight` (default), `bottomLeft`, `bottomRight`.

<Canvas of={PositionStories.Default} />
<Controls of={PositionStories.Default} />
