import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as SizeStories from './sy-badge-size.stories';

<Meta of={SizeStories} />

# Size

The `size` attribute controls the badge size. Values: `small`, `medium` (default).

<Canvas of={SizeStories.Default} />
<Controls of={SizeStories.Default} />
