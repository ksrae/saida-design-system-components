import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as DotStories from './sy-badge-dot.stories';

<Meta of={DotStories} />

# Dot

The `dot` attribute renders the badge as a small dot without a number.

<Canvas of={DotStories.Default} />
<Controls of={DotStories.Default} />
