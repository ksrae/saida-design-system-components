import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as BadgeStories from './sy-badge.stories';

<Meta of={BadgeStories} />

# Badge

A badge component for displaying notifications, counts, or status indicators that can be attached to other UI elements or displayed standalone.

## Default

The default badge shows a count attached to an icon.

<Canvas of={BadgeStories.Default} />
<Controls of={BadgeStories.Default} />