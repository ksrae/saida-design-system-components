import { Canvas, Meta, Controls, Story } from '@storybook/blocks';

import * as PopoverManualControlStories from './popover-manual-control.stories';

<Meta of={PopoverManualControlStories} />

# Manual Control
## Definition
Popover can be controlled via API: `setOpen()`, `setClose()`.
- `setOpen()`: Opens the popover manually.
- `setClose()`: Closes the popover manually.

This function works when the trigger attribute is set to `null`.

## Example
<Canvas of={PopoverManualControlStories.Param} />

<Controls of={PopoverManualControlStories.Param} />

