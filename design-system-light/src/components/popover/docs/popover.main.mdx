import { Canvas, Meta, Controls, Story } from "@storybook/blocks";

import * as PopoverParamStory from "./popover.stories";

<Meta of={PopoverParamStory} />

# Popover
## Definition
The popover element is a utility that lets you declaratively anchor `popover` containers to another element.<br/>
The value of the parent element's position attribute must be <b>relative</b>.


## Example
<Canvas of={PopoverParamStory.Default} />

<Controls of={PopoverParamStory.Default} />
