import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as GhostStories from './sy-collapse-ghost.stories';

<Meta of={GhostStories} />

# Ghost

The `ghost` attribute removes background and borders.

<Canvas of={GhostStories.Default} />
<Controls of={GhostStories.Default} />
