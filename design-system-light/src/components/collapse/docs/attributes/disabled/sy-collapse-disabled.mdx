import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as DisabledStories from './sy-collapse-disabled.stories';

<Meta of={DisabledStories} />

# Disabled

The `disabled` attribute prevents panels from expanding.

<Canvas of={DisabledStories.Default} />
<Controls of={DisabledStories.Default} />
