import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as BorderlessStories from './sy-collapse-borderless.stories';

<Meta of={BorderlessStories} />

# Borderless

The `borderless` attribute removes the panel border.

<Canvas of={BorderlessStories.Default} />
<Controls of={BorderlessStories.Default} />
