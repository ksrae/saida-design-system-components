import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './sy-modal-variant.stories';

<Meta of={Stories} />

# Variant

The `variant` attribute switches between `modal` (larger, maximizable) and `dialog` (compact confirmation style).

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />
