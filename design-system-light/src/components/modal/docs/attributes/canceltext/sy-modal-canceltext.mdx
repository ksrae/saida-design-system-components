import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './sy-modal-canceltext.stories';

<Meta of={Stories} />

# Cancel Text

The `cancelText` attribute overrides the label on the modal's cancel button.

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />
