import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './sy-modal-closable.stories';

<Meta of={Stories} />

# Closable

The `closable` attribute renders an ✕ close button on the right side of the modal header.

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />
