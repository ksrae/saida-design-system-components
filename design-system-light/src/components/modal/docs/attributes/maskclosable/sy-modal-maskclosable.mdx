import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './sy-modal-maskclosable.stories';

<Meta of={Stories} />

# Mask Closable

The `maskClosable` attribute closes the modal when the user clicks the dimmed background.

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />
