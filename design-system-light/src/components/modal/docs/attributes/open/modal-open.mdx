import { Canvas, Meta, Controls, Story } from "@storybook/blocks";
import * as ModalOpenStories from "./modal-open.stories";

<Meta of={ModalOpenStories} />

# Open
## Definition
Open the modal using the `open` property.<br/>
It is possible to set modal properties and values before opening it. <br/>
Removing `open` attribute will close the modal instantly without emitting any events.

## Boolean Attribute Handling
In the source code, <b>open = ""</b> should be modified as follows
- Set `open` attribute and don't put a value after it.
```
<sy-modal open></sy-modal>
```

- Alternatively, you can explicitly set it to true, but to not apply `open`, remove the attribute instead of setting its value to false.

```
<sy-modal open="true"></sy-modal>
```

## Example
<Canvas of={ModalOpenStories.Param} />

<Controls of={ModalOpenStories.Param} />

