import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './sy-modal-slot.stories';

<Meta of={Stories} />

# Slots

Modal exposes three named slots — `header`, `body`, `footer`. Override any combination to fully customize content.

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />
