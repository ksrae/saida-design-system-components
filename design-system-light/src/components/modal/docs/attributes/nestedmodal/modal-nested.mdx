import { Canvas, Meta, Controls, Story } from "@storybook/blocks";
import * as ModalNestedStories from "./modal-nested.stories";

<Meta of={ModalNestedStories} />

# Nested Modal
## Definition
Modals can call other modals on top of them. <br/>
It doesn't matter where the nested modal code is located. <br/>
The nested modal code is successfully loaded even though it is in, or out of the modal tag. <br/>


## Example
<Canvas of={ModalNestedStories.Param} />

<Controls of={ModalNestedStories.Param} />
