import { Canvas, Meta, Controls, Story } from "@storybook/blocks";
import * as ModalWidthStories from "./modal-width.stories";

<Meta of={ModalWidthStories} />

# Width
## Definition
Sets a fixed width, regardless of the width of contents of the modal.<br/>
Only numeric values are acceptable for the width property, and the number will be applied as pixels(px). <br/>
If no width is specified, a width that fits the contents of the modal is provided.

## Example
<Canvas of={ModalWidthStories.Param} />

<Controls of={ModalWidthStories.Param} />

