import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './sy-modal-width.stories';

<Meta of={Stories} />

# Width

The `width` attribute sets the modal width in pixels.

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />
