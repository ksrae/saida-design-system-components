import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './sy-modal-position.stories';

<Meta of={Stories} />

# Position

`top` and `left` attributes position the modal manually in pixels from the viewport edges. Default (`-1`) keeps the modal centered.

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />
