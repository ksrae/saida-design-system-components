import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as Stories from './sy-modal-oktext.stories';

<Meta of={Stories} />

# Ok Text

The `okText` attribute overrides the label on the modal's confirm/OK button.

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />
