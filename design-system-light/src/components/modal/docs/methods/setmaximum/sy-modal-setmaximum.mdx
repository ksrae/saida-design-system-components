import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as Stories from './sy-modal-setmaximum.stories';

<Meta of={Stories} />

# setMaximum()

Toggles the maximized state of the modal. Only available when `variant="modal"` and `enableModalMaximize` is true.

<Canvas of={Stories.Default} />
