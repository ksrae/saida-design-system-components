import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as Stories from './sy-modal-setok.stories';

<Meta of={Stories} />

# setOk(value?)

Fires the OK confirmation flow. The `closed` event is emitted with `{ event: 'ok', value }`.

<Canvas of={Stories.Default} />
