import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as LabelStories from "./sy-label.stories";

<Meta of={LabelStories} />

# Label
## Definition
The label component is used to provide descriptive text for form elements, enhancing accessibility and usability.

## Example
<Canvas of={LabelStories.Default} />

<Controls of={LabelStories.Default} />