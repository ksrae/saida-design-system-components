import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as WidthStories from './sy-label-width.stories';

<Meta of={WidthStories} />

# Width

The `width` attribute sets a fixed width for the label area (e.g., `120px`, `10rem`).

<Canvas of={WidthStories.Default} />
<Controls of={WidthStories.Default} />
