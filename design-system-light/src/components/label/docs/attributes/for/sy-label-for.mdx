import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as ForStories from './sy-label-for.stories';

<Meta of={ForStories} />

# For

The `for` attribute links the label to the input whose `id` matches — clicking the label focuses the input.

<Canvas of={ForStories.Default} />
