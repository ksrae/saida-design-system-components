import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as EmptyParamStory from "./sy-empty.stories";

<Meta of={EmptyParamStory} />

# Empty
## Definition
The `Empty` element in a design system is a visual representation displayed when there is no content to show in a particular section or page.

## Example
<Canvas of={EmptyParamStory.Default} />

<Controls of={EmptyParamStory.Default} />
