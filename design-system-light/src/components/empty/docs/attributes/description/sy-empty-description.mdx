import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as DescriptionStories from './sy-empty-description.stories';

<Meta of={DescriptionStories} />

# Description

The `description` attribute shows a text message below the empty illustration.

<Canvas of={DescriptionStories.Default} />
<Controls of={DescriptionStories.Default} />
