import { Canvas, Meta, Controls, Story } from "@storybook/blocks";
import * as ModelessStatusEventStories from "./modeless-status-event.stories";

<Meta of={ModelessStatusEventStories} />

# Status Event
## Definition
Emit the <b>status</b> event when the modeless is changed status.<br/>

```
event: {
  detail: string; // The status of the modeless, can be 'maximum', 'minimum', 'restore'.
}
```

## Example
<Canvas of={ModelessStatusEventStories.Param} />

<Controls of={ModelessStatusEventStories.Param} />
