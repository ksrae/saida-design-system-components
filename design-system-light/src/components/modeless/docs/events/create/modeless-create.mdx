import { Canvas, Meta, Controls, Story } from "@storybook/blocks";
import * as ModelessCreateStories from "./modeless-create.stories";

<Meta of={ModelessCreateStories} />

# Create
## Definition
The function in the Modeless group creates a modeless instance. <br/>
A unique ID value must be provided.<br/>

To get the results of modified options in the test, the <b>Create Modeless</b> button must be clicked; <br/>
otherwise, default values will be applied.

## Example
<Canvas of={ModelessCreateStories.Param} />

<Controls of={ModelessCreateStories.Param} />
