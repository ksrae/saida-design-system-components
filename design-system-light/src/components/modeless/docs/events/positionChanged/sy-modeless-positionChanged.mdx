import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as EventStories from './sy-modeless-positionChanged.stories';

<Meta of={EventStories} />

# PositionChanged

The `positionChanged` event.

<Canvas of={EventStories.Default} />