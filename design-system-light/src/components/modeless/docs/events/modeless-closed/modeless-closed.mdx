import { Canvas, Meta, Controls, Story } from "@storybook/blocks";
import * as ModelessClosedStories from "./modeless-closed.stories";

<Meta of={ModelessClosedStories} />

# Closed
## Definition
Emit the <b>closed</b> event when the modeless is closed.<br/>

```
event: {
  detail: {
    id: string; // The id of the modeless
  }
}
```

## Example
<Canvas of={ModelessClosedStories.Param} />

<Controls of={ModelessClosedStories.Param} />
