import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as ModelessStories from './sy-modeless.stories';
import * as ModelessGroupStories from './sy-modeless-group.stories';

<Meta of={ModelessStories} />

# Modeless

A floating, draggable, resizable modeless window. Use `sy-modeless` for a single window or `sy-modeless-group` to manage multiple windows programmatically.

## Modeless

<Canvas of={ModelessStories.Default} />
<Controls of={ModelessStories.Default} />

## Modeless Group

<Canvas of={ModelessGroupStories.Default} />
<Controls of={ModelessGroupStories.Default} />
