import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as MethodStories from './sy-modeless-group-updateContent.stories';

<Meta of={MethodStories} />

# UpdateContent

The `updateContent` method.

<Canvas of={MethodStories.Default} />