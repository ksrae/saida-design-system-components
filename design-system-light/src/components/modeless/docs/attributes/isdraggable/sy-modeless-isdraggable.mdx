import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-modeless-isdraggable.stories';

<Meta of={AttrStories} />

# Draggable

The `draggable` attribute. When `true`, the header is shown and the user can
drag the modeless window by the header.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />