import { Canvas, Meta, Controls, Story } from "@storybook/blocks";
import * as ModelessOpenStories from "./modeless-open.stories";

<Meta of={ModelessOpenStories} />

# Open
## Definition
Determines the visibility of the modeless.<br/>
The modeless which does not created by modeless group cannot be removed by close function of the modeless group.


## Boolean Attribute Handling
In the source code, <b>open = ""</b> should be modified as follows
- Set `open` attribute and don't put a value after it.
```
<sy-modeless open></sy-modeless>
```

- Alternatively, you can explicitly set it to true, but to not apply `open`, remove the attribute instead of setting its value to false.

```
<sy-modeless open="true"></sy-modeless>
```


## Example
<Canvas of={ModelessOpenStories.Param} />

<Controls of={ModelessOpenStories.Param} />
