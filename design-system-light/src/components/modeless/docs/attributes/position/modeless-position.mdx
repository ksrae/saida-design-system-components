import { Canvas, Meta, Controls, Story } from "@storybook/blocks";
import * as ModelessPositionStories from "./modeless-position.stories";

<Meta of={ModelessPositionStories} />

# Position
## Definition
Adjusts the top and left positions of the Modeless.
Only numeric values are acceptable for the `top` and `left` property, and the number will be applied as pixels(px). <br/>

## Example
<Canvas of={ModelessPositionStories.Param} />

<Controls of={ModelessPositionStories.Param} />
