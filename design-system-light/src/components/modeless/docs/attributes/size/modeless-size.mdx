import { Canvas, Meta, Controls, Story } from "@storybook/blocks";
import * as ModelessSizeStories from "./modeless-size.stories";

<Meta of={ModelessSizeStories} />

# Size
## Definition
Adjusts the `height` and `width` of the Modeless.<br/>
Only numeric values are acceptable for the `width` and `height` property, and the numbers will be applied as pixels(px). <br/>
The `resizable` attribute must be set.
## Example
<Canvas of={ModelessSizeStories.Param} />

<Controls of={ModelessSizeStories.Param} />
