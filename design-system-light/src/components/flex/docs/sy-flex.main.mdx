import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as FlexStories from './sy-flex.stories';

<Meta of={FlexStories} />

# Flex

## Definition
A flexbox layout container with configurable direction, alignment, gaps, wrap, and padding.

## Example
<Canvas of={FlexStories.Default} />
<Controls of={FlexStories.Default} />
