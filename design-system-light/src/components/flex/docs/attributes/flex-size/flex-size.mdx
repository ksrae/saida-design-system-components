import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as FlexSizeStories from './flex-size.stories';

<Meta of={FlexSizeStories} />

# Flex Size
## Definition
The `height` and `width` attributes define the size of the flex container's children.

## Values
`height` and `width` can be set to string type number such as '100' or set with pixel like '100px' & 100%.

## Example
<Canvas of={FlexSizeStories.Param} />

<Controls of={FlexSizeStories.Param} />
