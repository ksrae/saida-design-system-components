import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as FlexGapStories from './flex-gap.stories';

<Meta of={FlexGapStories} />

# Flex Gap
## Definition
The `columnGap` and `rowGap` attributes define the horizontal and vertical space between the flex container's children.

## Values
- xsmall: 4px
- small: 8px
- medium: 16px
- large: 24px
- xlarge: 32px

## Example
<Canvas of={FlexGapStories.Param} />

<Controls of={FlexGapStories.Param} />
