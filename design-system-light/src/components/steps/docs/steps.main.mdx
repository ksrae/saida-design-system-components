import { Canvas, Meta, Controls, Story } from "@storybook/blocks";

import * as StepsParamStory from "./steps.stories";
import * as StepParamStory from "./step.stories";

<Meta of={StepsParamStory} />

# Steps
## Definition
Steps visually represents a multi-step process, guiding users through a sequence of steps or stages. <br/>
Each step typically includes a title, description, and a visual indicator (such as a number or icon) to help users understand their progress within a process.


## Example
<Canvas of={StepsParamStory.Default} />

<Controls of={StepsParamStory.Default} />


# Step item
## Definition
Step item for all the steps.


## Example
<Canvas of={StepParamStory.Default} />
<Controls of={StepParamStory.Default} />
