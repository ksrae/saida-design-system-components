import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as GlobalHeaderStories from './sy-global-header.stories';

<Meta of={GlobalHeaderStories} />

# Global Header

## Definition
Top application header with title, tabs, and built-in action icons (search, information, notification).

## Example
<Canvas of={GlobalHeaderStories.Default} />
<Controls of={GlobalHeaderStories.Default} />
