import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as SelectRemovedStories from './select-removed.stories';

<Meta of={SelectRemovedStories} />

# Removed
## Definition
The select emits <b>removed</b> event when an item is removed from the selection.<br/>


```
event: {
  detail: {item, selectedOptions: this.selectedOptions, isValid: this.isValid, status: this.validStatus},
}
```

## Example
<Canvas of={SelectRemovedStories.Param} />

<Controls of={SelectRemovedStories.Param} />

