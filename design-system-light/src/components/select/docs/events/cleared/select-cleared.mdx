import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as SelectClearedStories from './select-cleared.stories';

<Meta of={SelectClearedStories} />

# Cleared
## Definition
The select emits <b>cleared</b> event when the selection is cleared.<br/>



## Example
<Canvas of={SelectClearedStories.Param} />

<Controls of={SelectClearedStories.Param} />

