import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as SelectLoadingStories from './select-loading.stories';

<Meta of={SelectLoadingStories} />

# Loading
## Definition
Showing loading icon when opening the select.<br/>
Any options are not allowed to added when this attribute is set.


## Boolean Attribute Handling
In the source code, <b>loading = ""</b> should be modified as follows
- Set `loading` attribute and don't put a value after it.
```
<sy-select loading></sy-select>
```
- Alternatively, you can explicitly set it to true, but to not apply `loading`, remove the attribute instead of setting its value to false.

```
<sy-select loading="true"></sy-select>
```

## Example
<Canvas of={SelectLoadingStories.Param} />

<Controls of={SelectLoadingStories.Param} />

