import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as SelectSizeStories from './select-size.stories';

<Meta of={SelectSizeStories} />

# Size
## Definition
Sets size of the select. <br/>
There are two types: `small`, `medium`, and `large`.<br/><br/>
The default value is `medium`.<br/>


## Example
<Canvas of={SelectSizeStories.Param} />

<Controls of={SelectSizeStories.Param} />

