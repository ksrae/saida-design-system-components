import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as SelectOptionCustomStories from './select-option-custom.stories';

<Meta of={SelectOptionCustomStories} />

# Option Slot
## Definition
The `slot` attribute allows you to define a custom slot for the `option` component within the `select`. <br/>
This enables you to insert custom content or components within the option, providing flexibility in how the option is rendered and what it contains.

## Example
<Canvas of={SelectOptionCustomStories.Param} />

<Controls of={SelectOptionCustomStories.Param} />

