import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-select-option-is-custom-tag.stories';

<Meta of={AttrStories} />

# Is Custom Tag

The `isCustomTag` attribute.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />