import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-select-selected-options.stories';

<Meta of={AttrStories} />

# Selected Options

The `selectedOptions` attribute.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />