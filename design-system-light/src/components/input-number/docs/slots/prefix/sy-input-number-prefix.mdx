import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as SlotStories from './sy-input-number-prefix.stories';

<Meta of={SlotStories} />

# Prefix Slot

Content projected via `slot="prefix"` appears before the numeric input.

<Canvas of={SlotStories.Default} />
