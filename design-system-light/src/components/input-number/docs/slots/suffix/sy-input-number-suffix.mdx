import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as SlotStories from './sy-input-number-suffix.stories';

<Meta of={SlotStories} />

# Suffix Slot

Content projected via `slot="suffix"` appears after the numeric input.

<Canvas of={SlotStories.Default} />
