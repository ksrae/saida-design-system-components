import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as MethodStories from './sy-input-number-get-status.stories';

<Meta of={MethodStories} />

# getStatus

Returns the current validation status key (empty string when valid).

<Canvas of={MethodStories.Default} />
