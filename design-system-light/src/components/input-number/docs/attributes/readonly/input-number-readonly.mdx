import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as InputNumberReadonlyStories from './input-number-readonly.stories';

<Meta of={InputNumberReadonlyStories} />

# Readonly
## Definition
Use the readonly attribute to set the input number readonly state.<br/>
It makes the input number element not mutable, meaning the user can not edit the control.

## Boolean Attribute Handling
In the source code, <b>readonly = ""</b> should be modified as follows
- Set `readonly` attribute and don't put a value after it.
```
<sy-input-number readonly></sy-input-number>

```
- Alternatively, you can explicitly set it to true but to not apply `readonly`, remove the attribute instead of setting its value to false.
```
<sy-input-number readonly="true"></sy-input-number>
```

## Example
<Canvas of={InputNumberReadonlyStories.Param} />

<Controls of={InputNumberReadonlyStories.Param} />
