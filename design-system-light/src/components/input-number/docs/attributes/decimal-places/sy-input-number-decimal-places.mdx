import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-input-number-decimal-places.stories';

<Meta of={AttrStories} />

# Decimal Places

Number of decimal places to display, with rounding applied on blur.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
