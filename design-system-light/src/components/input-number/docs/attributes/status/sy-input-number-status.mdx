import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-input-number-status.stories';

<Meta of={AttrStories} />

# Status

Validation status styling (`default`, `warning`, `error`, `success`).

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
