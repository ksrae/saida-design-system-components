import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-input-number-rounding.stories';

<Meta of={AttrStories} />

# Rounding

How decimal values are rounded: `round`, `ceil`, or `floor`.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
