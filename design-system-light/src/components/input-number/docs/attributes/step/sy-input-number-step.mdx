import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-input-number-step.stories';

<Meta of={AttrStories} />

# Step

Increment/decrement step applied when using the stepper or arrow keys.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
