import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as InputNumberDisabledStories from './input-number-disabled.stories';

<Meta of={InputNumberDisabledStories} />

# Disabled
## Definition
Disables input number with disabled attribute.<br/>
If disabled attribute is set, any inputs are not allowed and events are prevented.

## Boolean Attribute Handling
In the source code, disabled = "" should be modified as follows
- Set `disabled` attribute and don't put a value after it.
```
<sy-input-number disabled></sy-input-number>
```
- Alternatively, you can explicitly set it  to `true`, but to not apply the `disabled`, remove the attribute instead of setting its value to`false`.

```
<sy-input-number disabled="true"></sy-input-number>
```

## Example
<Canvas of={InputNumberDisabledStories.Param} />

<Controls of={InputNumberDisabledStories.Param} />
