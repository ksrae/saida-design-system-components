import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-input-number-borderless.stories';

<Meta of={AttrStories} />

# Borderless

Removes the visible border for a minimal look.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
