import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as MenuOpenedStories from './menu-opened.stories';

<Meta of={MenuOpenedStories} />

# Opened
## Definition
The menu emits the <b>opened</b> event when it is opened or closed by the user.<br/>

```
event: {
  detail: boolean; // true if the menu is opened, false if it is closed
}
```

## Example
<Canvas of={MenuOpenedStories.Param} />

<Controls of={MenuOpenedStories.Param} />
