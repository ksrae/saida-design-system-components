import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as EventStories from './sy-menu-opened.stories';

<Meta of={EventStories} />

# opened

Fires whenever the menu's open state changes. Detail is a boolean — `true`
when opened, `false` when closed.

<Canvas of={EventStories.Default} />
