import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-menu-loading.stories';

<Meta of={AttrStories} />

# Loading

Replaces item rendering with a spinner while async content is being fetched.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
