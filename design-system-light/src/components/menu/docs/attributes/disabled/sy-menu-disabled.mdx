import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-menu-disabled.stories';

<Meta of={AttrStories} />

# Disabled

Disables the menu as a whole — trigger interactions and item clicks are ignored.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
