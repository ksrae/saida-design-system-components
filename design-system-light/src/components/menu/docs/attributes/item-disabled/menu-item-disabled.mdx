import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as MenuItemDisabledStories from './menu-item-disabled.stories';

<Meta of={MenuItemDisabledStories} />

# Menu Item Disabled
## Definition
Use the disabled attribute to a menu item.<br/>
If disabled attribute is set true, all events will be prevented.

## Boolean Attribute Handling
In the source code, disabled = "" should be modified as follows
- Set `disabled` attribute and don't put a value after it.
```
<sy-menu-item disabled></sy-menu-item>
```

- Alternatively, you can explicitly set it  to `true`, but to not apply the `disabled`, remove the attribute instead of setting its value to`false`.
```
<sy-menu-item disabled="true"></sy-menu-item>
```

## Example
<Canvas of={MenuItemDisabledStories.Param} />

<Controls of={MenuItemDisabledStories.Param} />
