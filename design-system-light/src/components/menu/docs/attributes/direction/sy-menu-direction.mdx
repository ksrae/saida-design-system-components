import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-menu-direction.stories';

<Meta of={AttrStories} />

# Direction

Submenu expansion direction — `left` pushes nested menus toward the start,
`right` toward the end.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
