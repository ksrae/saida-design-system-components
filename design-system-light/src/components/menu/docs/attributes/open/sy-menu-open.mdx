import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as AttrStories from './sy-menu-open.stories';

<Meta of={AttrStories} />

# Open

Controlled boolean that forces the menu open. Pair with `trigger="none"` if
you want to fully drive visibility from the host app.

<Canvas of={AttrStories.Default} />
<Controls of={AttrStories.Default} />
