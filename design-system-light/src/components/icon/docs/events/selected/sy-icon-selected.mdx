import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as SelectedStories from './sy-icon-selected.stories';

<Meta of={SelectedStories} />

# selected

The `selected` event fires when a `selectable` icon is clicked. Detail: `{ value }`.

<Canvas of={SelectedStories.Default} />
