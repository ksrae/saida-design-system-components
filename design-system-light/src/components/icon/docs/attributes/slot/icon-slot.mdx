import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as IconSlotStories from './icon-slot.stories';

<Meta of={IconSlotStories} />

# Slot
## Definition
To use custom SVG icons, inserting SVG content directly into the icon component. <br/>
This is useful for rendering icons that are not part of the default icon library and customizing the SVG content dynamically.<br/>


## Example
<Canvas of={IconSlotStories.Param} />

<Controls of={IconSlotStories.Param} />
