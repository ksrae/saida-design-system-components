import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as IconPathStories from './icon-path.stories';

<Meta of={IconPathStories} />

# Path
## Definition
The `path` attribute allows to set link to use custom SVG icons that are not part of the default icon library.<br/>


## Example
<Canvas of={IconPathStories.Param} />

<Controls of={IconPathStories.Param} />
