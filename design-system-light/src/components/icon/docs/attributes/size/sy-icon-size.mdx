import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as SizeStories from './sy-icon-size.stories';

<Meta of={SizeStories} />

# Size

The `size` attribute ranges from `xxsmall` to `xxxlarge` (default `medium`).

<Canvas of={SizeStories.Default} />
<Controls of={SizeStories.Default} />
