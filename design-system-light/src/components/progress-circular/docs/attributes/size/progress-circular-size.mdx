import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as ProgressCircularSizeParams from './progress-circular-size.stories';

<Meta of={ProgressCircularSizeParams} />

# Size
## Definition
The size of the progress circular. 

## Values 
The value should be one of the following: `small`, `medium`, `large`.<br/>
Default Value is `medium`.



## Example
<Canvas of={ProgressCircularSizeParams.Param} />

<Controls of={ProgressCircularSizeParams.Param} />

