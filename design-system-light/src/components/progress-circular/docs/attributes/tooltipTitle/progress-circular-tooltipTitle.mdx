import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as ProgressCircularTooltipTitleParams from './progress-circular-tooltipTitle.stories';

<Meta of={ProgressCircularTooltipTitleParams} />

# TooltipTitle
## Definition
The `tooltipTitle` property is used to set the title of the tooltip that appears when hovering over the progress circular component. <br/>
The tooltip provides additional information about the progress circular. <br/>

## Example
<Canvas of={ProgressCircularTooltipTitleParams.Param} />

<Controls of={ProgressCircularTooltipTitleParams.Param} />

