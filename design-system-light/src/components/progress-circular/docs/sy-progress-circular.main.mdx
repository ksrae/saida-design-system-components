import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as ProgressCircularStories from './sy-progress-circular.stories';

<Meta of={ProgressCircularStories} />

# Progress Circular

## Definition
A circular progress indicator with determinate or indeterminate states.

## Example
<Canvas of={ProgressCircularStories.Default} />
<Controls of={ProgressCircularStories.Default} />
