import { Canvas, Meta, Controls, Story } from "@storybook/blocks";
import * as ProgressCircularParamStory from "./progress-circular.stories";

<Meta of={ProgressCircularParamStory} />

# Progress Circluar
## Definition
A `Progress Circular` indicator is a visual representation of a task's progress in the form of a circle. 
It is used to indicate the progress of a task or process in a visual way.



## Example
<Canvas of={ProgressCircularParamStory.Default} />

<Controls of={ProgressCircularParamStory.Default} />
