import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as ProgressBarStories from './sy-progress-bar.stories';

<Meta of={ProgressBarStories} />

# Progress Bar

## Definition
A horizontal progress indicator with determinate or indeterminate states and optional segments.

## Example
<Canvas of={ProgressBarStories.Default} />
<Controls of={ProgressBarStories.Default} />
