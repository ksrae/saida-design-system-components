import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as ProgressTooltipTitleParams from './progressbar-tooltipTitle.stories';

<Meta of={ProgressTooltipTitleParams} />

# TooltipTitle
## Definition
The `tooltipTitle` property is used to set the title of the tooltip that appears when hovering over the progress bar component. <br/>
The tooltip provides additional information about the progress bar. <br/>


## Example
<Canvas of={ProgressTooltipTitleParams.Param} />

<Controls of={ProgressTooltipTitleParams.Param} />

