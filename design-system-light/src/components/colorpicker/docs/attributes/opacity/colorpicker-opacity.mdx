import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as ColorpickerOpacityStories from './colorpicker-opacity.stories';


<Meta of={ColorpickerOpacityStories} />

# Opacity
## Definition
The `opacity` attribute allows you to set the opacity level of the color in the `colorpicker`. <br/>
This can be a value between `0` (fully transparent) and `1` (fully opaque). <br/>



## Example
<Canvas of={ColorpickerOpacityStories.Param} />

<Controls of={ColorpickerOpacityStories.Param} />
