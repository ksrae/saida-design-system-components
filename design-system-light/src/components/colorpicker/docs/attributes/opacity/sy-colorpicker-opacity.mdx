import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as OpacityStories from './sy-colorpicker-opacity.stories';

<Meta of={OpacityStories} />

# Opacity

The `opacity` attribute controls the alpha channel of the picked color (0 – 1).

<Canvas of={OpacityStories.Default} />
<Controls of={OpacityStories.Default} />
