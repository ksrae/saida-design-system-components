import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as HideOpacityStories from './sy-colorpicker-hideopacity.stories';

<Meta of={HideOpacityStories} />

# Hide Opacity

The `hideOpacity` attribute hides the opacity slider when set to `true`.

<Canvas of={HideOpacityStories.Default} />
<Controls of={HideOpacityStories.Default} />
