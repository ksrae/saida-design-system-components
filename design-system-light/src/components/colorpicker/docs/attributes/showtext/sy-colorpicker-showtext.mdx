import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as ShowTextStories from './sy-colorpicker-showtext.stories';

<Meta of={ShowTextStories} />

# Show Text

The `showText` attribute renders the current color value as text next to the color preview.

<Canvas of={ShowTextStories.Default} />
<Controls of={ShowTextStories.Default} />
