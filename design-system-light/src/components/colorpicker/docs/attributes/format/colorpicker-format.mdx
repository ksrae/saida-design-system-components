import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as ColorpickerFormatStories from './colorpicker-format.stories';


<Meta of={ColorpickerFormatStories} />

# Format
## Definition
The `format` attribute allows you to specify the format of the color value in the `colorpicker`. <br/>
This can include options like `hex`, `rgb`, or `hsl`.






## Example
<Canvas of={ColorpickerFormatStories.Param} />

<Controls of={ColorpickerFormatStories.Param} />
