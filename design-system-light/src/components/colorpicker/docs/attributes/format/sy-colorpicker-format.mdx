import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as FormatStories from './sy-colorpicker-format.stories';

<Meta of={FormatStories} />

# Format

The `format` attribute sets the color value representation. Supported values are `hex`, `hsb`, and `rgb`.

<Canvas of={FormatStories.Default} />
<Controls of={FormatStories.Default} />
