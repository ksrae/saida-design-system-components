import { Canvas, Meta, Controls, Story } from "@storybook/blocks";

import * as ColorpickerParamStory from "./colorpicker.stories";

<Meta of={ColorpickerParamStory} />

# Colorpicker
## Definition
The Colorpicker component is a user interface element that allows users to select a color. <br/>
It typically consists of a color palette or spectrum from which users can choose a color, and it may also include an input field for entering color values in various formats <b>(e.g., hex, hsb, rgb)</b>.

## Example
<Canvas of={ColorpickerParamStory.Default} />

<Controls of={ColorpickerParamStory.Default} />


