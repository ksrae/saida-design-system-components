import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as InlineMessageStories from './sy-inline-message.stories';

<Meta of={InlineMessageStories} />

# Inline Message

## Definition
An inline popup message attached to a target with an action button.

## Example
<Canvas of={InlineMessageStories.Default} />
<Controls of={InlineMessageStories.Default} />
