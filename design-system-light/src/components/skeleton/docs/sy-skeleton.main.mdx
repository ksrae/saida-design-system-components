import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as SkeletonStories from './sy-skeleton.stories';

<Meta of={SkeletonStories} />

# Skeleton

## Definition
A placeholder preview of content used while data is being loaded.

## Example
<Canvas of={SkeletonStories.Default} />
<Controls of={SkeletonStories.Default} />
