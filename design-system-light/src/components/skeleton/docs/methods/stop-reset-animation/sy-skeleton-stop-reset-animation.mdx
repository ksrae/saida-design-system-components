import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as MethodStories from './sy-skeleton-stop-reset-animation.stories';

<Meta of={MethodStories} />

# Stop & Reset Animation

Stop and reset the shimmer animation via ``stopAnimation()`` and ``resetAnimation()``.

<Canvas of={MethodStories.Default} />