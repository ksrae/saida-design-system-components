import { Canvas, Meta, Controls, Story } from '@storybook/blocks';

import * as PopconfirmArrowStories from './popconfirm-arrow.stories';

<Meta of={PopconfirmArrowStories} />

# Arrow
## Definition
Add an arrow to your popconfirm with the arrow attribute. <br/>
Arrow point will be at center of the popconfirm.


## Boolean Attribute Handling
In the source code, <b> arrow = "" </b> should be modified as follows
- Set `arrow` attribute and don't put a value after it.
```
<sy-popconfirm arrow></sy-popconfirm>
```
- Alternatively, you can explicitly set it to true, but to not apply `arrow`, remove the attribute instead of setting its value to false.

```
<sy-popconfirm arrow="true"></sy-popconfirm>
```


## Example
<Canvas of={PopconfirmArrowStories.Param} />

<Controls of={PopconfirmArrowStories.Param} />

