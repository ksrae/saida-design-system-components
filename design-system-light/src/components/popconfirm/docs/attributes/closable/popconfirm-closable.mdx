import { Canvas, Meta, Controls, Story } from '@storybook/blocks';

import * as PopconfirmClosableStories from './popconfirm-closable.stories';

<Meta of={PopconfirmClosableStories} />

# Closable
## Definition
This attribute allows closing the popconfirm by clicking outside of it.<br/>
Closing popconfirm with a document click works only when the `closable` attribute is set.

## Boolean Attribute Handling
In the source code, <b>closable = ""</b> should be modified as follows
- Set `closable` attribute and don't put a value after it.
```
<sy-popconfirm closable></sy-popconfirm>
```
- Alternatively, you can explicitly set it to true, but to not apply `closable`, remove the attribute instead of setting its value to false.

```
<sy-popconfirm closable="true"></sy-popconfirm>
```

## Example
<Canvas of={PopconfirmClosableStories.Param} />

<Controls of={PopconfirmClosableStories.Param} />

