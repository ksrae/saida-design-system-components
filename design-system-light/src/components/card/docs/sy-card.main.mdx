import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as CardStories from './sy-card.stories';

<Meta of={CardStories} />

# Card

## Definition
A flexible container with optional cover, header, body and footer slots. Supports backdrop styling and an optional collapse toggle.

## Example
<Canvas of={CardStories.Default} />
<Controls of={CardStories.Default} />
