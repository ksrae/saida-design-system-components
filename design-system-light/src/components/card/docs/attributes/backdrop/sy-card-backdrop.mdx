import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as BackdropStories from './sy-card-backdrop.stories';

<Meta of={BackdropStories} />

# Backdrop

Applies a subtle surface backdrop to the card for visual separation.

<Canvas of={BackdropStories.Default} />
<Controls of={BackdropStories.Default} />
