import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as CollapsibleStories from './sy-card-collapsible.stories';

<Meta of={CollapsibleStories} />

# Collapsible

Adds a toggle icon that collapses and expands the card body.

<Canvas of={CollapsibleStories.Default} />
<Controls of={CollapsibleStories.Default} />
