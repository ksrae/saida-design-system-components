import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as RadioButtonCheckedStories from './radio-button-checked.stories';


<Meta of={RadioButtonCheckedStories} />

# Checked
## Definition
Sets the checked state of a single button within the radio group.


## Boolean Attribute Handling
In the source code, <b>checked = ""</b> should be modified as follows
- Set `checked` attribute and don't put a value after it.
```
<sy-radio-button checked></sy-radio-button>
```
- Alternatively, you can explicitly set it to `true`, but to not apply `checked`, remove the attribute instead of setting its value to `false`.

```
<sy-radio-button checked="true"></sy-radio-button>
```


## Example
<Canvas of={RadioButtonCheckedStories.Param} />

<Controls of={RadioButtonCheckedStories.Param} />

