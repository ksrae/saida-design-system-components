import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as RadioButtonGroupDefaultValueStories from './radio-button-group-defaultValue.stories';


<Meta of={RadioButtonGroupDefaultValueStories} />

# Group DefaultValue
## Definition
The `defaultValue` of the radio group selects the radio button which has the same value.<br/>
No buttons are selected if values do not match.


## Example
<Canvas of={RadioButtonGroupDefaultValueStories.Param} />

<Controls of={RadioButtonGroupDefaultValueStories.Param} />
