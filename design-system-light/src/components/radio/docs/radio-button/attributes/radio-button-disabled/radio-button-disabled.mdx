import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as RadioButtonDisabledStories from './radio-button-disabled.stories';


<Meta of={RadioButtonDisabledStories} />

# Disabled
## Definition
Disables a single radio button in a radio group.


## Boolean Attribute Handling
In the source code, <b>disabled = ""</b> should be modified as follows
- Set `disabled` attribute and don't put a value after it.
```
<sy-radio-button disabled></sy-radio-button>
```
- Alternatively, you can explicitly set it  to `true`, but to not apply the `disabled`, remove the attribute instead of setting its value to`false`.

```
<sy-radio-button disabled="true"></sy-radio-button>
```


## Example
<Canvas of={RadioButtonDisabledStories.Param} />

<Controls of={RadioButtonDisabledStories.Param} />

