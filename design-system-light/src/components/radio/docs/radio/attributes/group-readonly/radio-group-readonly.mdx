import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as RadioGroupReadonlyStories from './radio-group-readonly.stories';

<Meta of={RadioGroupReadonlyStories} />

# Readonly
## Definition
Determines the readonly status of all radios in a radio group.

## Boolean Attribute Handling
In the source code, <b>readonly = ""</b> should be modified as follows
- Set `readonly` attribute and don't put a value after it.
```
<sy-radio-group readonly></sy-radio-group>
```
- Alternatively, you can explicitly set it to true but to not apply `readonly`, remove the attribute instead of setting its value to false.

```
<sy-radio-group readonly="true"></sy-radio-group>
```


## Example
<Canvas of={RadioGroupReadonlyStories.Param} />

<Controls of={RadioGroupReadonlyStories.Param} />

