import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as RadioGroupPositionStories from './radio-group-position.stories';

<Meta of={RadioGroupPositionStories} />

# Group Position
## Definition
There are two group positions available for the radio group component: `horizontal` and `vertical`. <br/>
The `horizontal` position displays the radio buttons in a single row, while the `vertical` position stacks them in a column.


## Example
<Canvas of={RadioGroupPositionStories.Param} />

<Controls of={RadioGroupPositionStories.Param} />
