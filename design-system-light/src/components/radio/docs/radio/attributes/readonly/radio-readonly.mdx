import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as RadioReadonlyStories from './radio-readonly.stories';


<Meta of={RadioReadonlyStories} />

# Readonly
## Definition
Sets the radio readonly state.
This allows users to copy labels but not interact with or change them.

## Boolean Attribute Handling
In the source code, <b>readonly = ""</b> should be modified as follows
- Set `readonly` attribute and don't put a value after it.
```
<sy-radio readonly></sy-radio>
```
- Alternatively, you can explicitly set it to true but to not apply `readonly`, remove the attribute instead of setting its value to false.

```
<sy-radio readonly="true"></sy-radio>
```


## Example
<Canvas of={RadioReadonlyStories.Param} />

<Controls of={RadioReadonlyStories.Param} />
