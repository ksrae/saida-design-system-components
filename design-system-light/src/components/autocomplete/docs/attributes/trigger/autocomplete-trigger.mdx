import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as AutoCompleteTriggerStories from './autocomplete-trigger.stories';


<Meta of={AutoCompleteTriggerStories} />

# Trigger
## Definition
There are two options: `click`, `input`.<br/><br/>
The trigger affects following the condition of opening options:
- `click`: opens suggested item list at focus. This is default.
- `default`: opens suggested item list when input any words

## Example
<Canvas of={AutoCompleteTriggerStories.Param} />

<Controls of={AutoCompleteTriggerStories.Param} />
