import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as DebounceStories from './sy-autocomplete-debounce.stories';

<Meta of={DebounceStories} />

# Debounce Time

The `debounceTime` attribute delays the filtering after each keystroke by the specified number of milliseconds.
Useful when fetching suggestions from a remote API to avoid excessive requests.

<Canvas of={DebounceStories.Default} />
<Controls of={DebounceStories.Default} />
