import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as CaseSensitiveStories from './sy-autocomplete-case-sensitive.stories';

<Meta of={CaseSensitiveStories} />

# Case Sensitive

The `caseSensitive` attribute controls whether the autocomplete filtering is case-sensitive.
When set to `true`, typing `app` will not match `Apple`. When `false` (default), the match is case-insensitive.

<Canvas of={CaseSensitiveStories.Default} />
<Controls of={CaseSensitiveStories.Default} />
