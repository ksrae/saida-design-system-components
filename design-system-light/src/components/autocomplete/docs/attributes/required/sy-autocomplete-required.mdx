import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as RequiredStories from './sy-autocomplete-required.stories';

<Meta of={RequiredStories} />

# Required

The `required` attribute marks the autocomplete as a required form field.
Submitting the form without a selected value will trigger browser-native or custom validation.

<Canvas of={RequiredStories.Default} />
<Controls of={RequiredStories.Default} />
