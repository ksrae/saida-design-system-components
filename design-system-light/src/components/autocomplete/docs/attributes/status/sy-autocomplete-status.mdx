import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as StatusStories from './sy-autocomplete-status.stories';

<Meta of={StatusStories} />

# Status

The `status` attribute controls the visual status of the autocomplete — a hint used for inline
validation feedback (`warning`, `error`, `success`) independent of form validity.

<Canvas of={StatusStories.Default} />
<Controls of={StatusStories.Default} />
