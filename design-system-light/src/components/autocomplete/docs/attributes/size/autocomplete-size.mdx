import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as AutoCompleteSizeStories from './autocomplete-size.stories';


<Meta of={AutoCompleteSizeStories} />

# Size
## Definition
Sets size of the autocomplete. <br/>
There are three types: `small`, `medium`, and `large`<br/><br/>
The default value is `medium`.<br/>

## Example
<Canvas of={AutoCompleteSizeStories.Param} />

<Controls of={AutoCompleteSizeStories.Param} />