import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as SizeStories from './sy-autocomplete-size.stories';

<Meta of={SizeStories} />

# Size

The `size` attribute controls the visual size of the autocomplete input.
Available values: `small`, `medium` (default), `large`.

<Canvas of={SizeStories.Default} />
<Controls of={SizeStories.Default} />
