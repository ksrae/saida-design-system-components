import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as SourceStories from './sy-autocomplete-source.stories';

<Meta of={SourceStories} />

# Source

The `source` attribute accepts an array of strings that populate the suggestion list.
Use the Controls panel to edit the array and see results in real time.

<Canvas of={SourceStories.Default} />
<Controls of={SourceStories.Default} />
