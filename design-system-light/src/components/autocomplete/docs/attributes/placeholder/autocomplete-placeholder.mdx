import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as AutoCompletePlaceholderStories from './autocomplete-placeholder.stories';


<Meta of={AutoCompletePlaceholderStories} />

# Placeholder
## Definition
The `placeholder` attribute in the `sy-autocomplete` component specifies a short hint that describes the expected value of the input field. <br/>
This hint is displayed in the input field before the user enters a value.

## Example
<Canvas of={AutoCompletePlaceholderStories.Param} />

<Controls of={AutoCompletePlaceholderStories.Param} />