import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as PlaceholderStories from './sy-autocomplete-placeholder.stories';

<Meta of={PlaceholderStories} />

# Placeholder

The `placeholder` attribute sets the hint text displayed inside the input when it is empty.

<Canvas of={PlaceholderStories.Default} />
<Controls of={PlaceholderStories.Default} />
