import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as LoadingStories from './sy-autocomplete-loading.stories';

<Meta of={LoadingStories} />

# Loading

The `loading` attribute displays a spinner inside the dropdown and hides the suggestion list.
Use this while fetching remote data asynchronously.

<Canvas of={LoadingStories.Default} />
<Controls of={LoadingStories.Default} />
