import { Canvas, Meta, Controls, Story } from "@storybook/blocks";

import * as AutoCompleteParamStory from "./autocomplete.stories";

<Meta of={AutoCompleteParamStory} />

# AutoComplete
## Definition
The Autocomplete element allows users to search for and display items, including words from the input, based on the source data.

## Example
<Canvas of={AutoCompleteParamStory.Default} />

<Controls of={AutoCompleteParamStory.Default} />
