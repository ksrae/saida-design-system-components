import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as MethodStories from './sy-tree-select-setCustomError.stories';

<Meta of={MethodStories} />

# SetCustomError

The `setCustomError` method.

<Canvas of={MethodStories.Default} />
