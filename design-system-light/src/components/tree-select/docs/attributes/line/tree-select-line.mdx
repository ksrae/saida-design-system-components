import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as TreeSelectLineStories from './tree-select-line.stories';

<Meta of={TreeSelectLineStories} />

# Line
## Definition
Determines whether add line to the tree select or not.<br/>

## Boolean Attribute Handling
In the source code, <b>line = ""</b> should be modified as follows
- Set `line` attribute and don't put a value after it.
```
<sy-tree-select line></sy-tree-select>
```
- Alternatively, you can explicitly set it to true, but to not apply `line`, remove the attribute instead of setting its value to false.

```
<sy-tree-select line="true"></sy-tree-select>
```

## Example
<Canvas of={TreeSelectLineStories.Param} />

<Controls of={TreeSelectLineStories.Param} />
