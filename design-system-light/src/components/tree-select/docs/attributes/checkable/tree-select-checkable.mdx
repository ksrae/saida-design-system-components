import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as TreeSelectCheckableStories from './tree-select-checkable.stories';

<Meta of={TreeSelectCheckableStories} />

# Checkable
## Definition
Determines whether add checkbox to all the tree items or not.<br/>
If set true, the tree does not close when any item is checked.


## Boolean Attribute Handling
In the source code, <b>checkable = ""</b> should be modified as follows
- Set `checkable` attribute and don't put a value after it.
```
<sy-tree-select checkable></sy-tree-select>
```
- Alternatively, you can explicitly set it to true, but to not apply `checkable`, remove the attribute instead of setting its value to false.

```
<sy-tree-select checkable="true"></sy-tree-select>
```

## Example
<Canvas of={TreeSelectCheckableStories.Param} />

<Controls of={TreeSelectCheckableStories.Param} />
