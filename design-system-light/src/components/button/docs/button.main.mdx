import { Canvas, Meta, Controls, Story } from "@storybook/blocks";
import * as ButtonParamStory from "./button.stories";

<Meta of={ButtonParamStory} />

# Button
## Definition
A button is a component used to trigger an action or perform a specific function within an interface. <br />
Button is accompanied by text or icons to indicate its purpose. It can vary in appearance based on its state.

## Example
<Canvas of={ButtonParamStory.Default} />

<Controls of={ButtonParamStory.Default} />
