import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as ButtonJustifiedStories from "./sy-button-justified.stories";

<Meta of={ButtonJustifiedStories} />

# Justified
This will force the button to fit the width of its parent. Use it if necessary, depending on the page design.

<Canvas of={ButtonJustifiedStories.Default} />
<Controls of={ButtonJustifiedStories.Default} />
