import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as ButtonSlotStories from './button-slot.stories';

<Meta of={ButtonSlotStories} />

# Value
## Definition
Values can include text only or text with HTML tags.
When adding HTML tags, be sure to keep security in mind.

## Example
<Canvas of={ButtonSlotStories.Param} />

<Controls of={ButtonSlotStories.Param} />
