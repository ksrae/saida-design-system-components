import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as ButtonSizeStories from "./sy-button-size.stories";

<Meta of={ButtonSizeStories} />

# Size
Sets size of the button. <br/>
There are three types: `small` , `medium`, and `large`<br/><br/>
The default value is `medium`.<br/>


<Canvas of={ButtonSizeStories.Default} />

<Controls of={ButtonSizeStories.Default} />
