import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as DisabledStories from "./sy-button-disabled.stories";

<Meta of={DisabledStories} />

# Disabled
The `disabled` attribute prevents user interaction with the button and visually indicates that the action is unavailable.

<Canvas of={DisabledStories.Default} />
<Controls of={DisabledStories.Default} />
