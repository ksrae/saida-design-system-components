import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as ButtonMainStories from "./sy-button.main.stories";

<Meta of={ButtonMainStories} />

# Button
The Button component is a fundamental interactive element that triggers actions when clicked.<br/>
It provides multiple variants and sizes to fit different use cases and visual hierarchies.

<Canvas of={ButtonMainStories.Default} />

<Controls of={ButtonMainStories.Default} />


