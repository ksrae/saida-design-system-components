import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as ClosableStories from './sy-drawer-closable.stories';

<Meta of={ClosableStories} />

# Closable

The `closable` attribute renders a close (×) button in the drawer header.

<Canvas of={ClosableStories.Default} />
<Controls of={ClosableStories.Default} />
