import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as DrawerSizeStories from './drawer-size.stories';

<Meta of={DrawerSizeStories} />

# Size
## Definition
Sets the size of the drawer. <br/>
There are four types: `small`, `medium`, `large`, and `custom`<br/>
To set customsize, set the size to `custom`.<br/>
The default value is `medium`.<br/>

## Example
<Canvas of={DrawerSizeStories.Param} />

<Controls of={DrawerSizeStories.Param} />
