import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as PositionStories from './sy-drawer-position.stories';

<Meta of={PositionStories} />

# Position

The `position` attribute pins the drawer to one side of the viewport: `top`, `left`, `right`, or `bottom`.

<Canvas of={PositionStories.Default} />
<Controls of={PositionStories.Default} />
