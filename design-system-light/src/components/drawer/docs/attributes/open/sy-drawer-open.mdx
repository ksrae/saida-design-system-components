import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as OpenStories from './sy-drawer-open.stories';

<Meta of={OpenStories} />

# Open

The `open` attribute controls the drawer visibility. Toggle it in code to show or hide the drawer.

<Canvas of={OpenStories.Default} />
<Controls of={OpenStories.Default} />
