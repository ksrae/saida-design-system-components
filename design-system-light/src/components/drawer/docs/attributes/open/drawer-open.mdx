import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as DrawerOpenStories from './drawer-open.stories';

<Meta of={DrawerOpenStories} />

# Open
## Definition
Setting the open property to true to open the drawer, and setting the open property to false to close it.


## Boolean Attribute Handling
In the source code, open = "" should be modified as follows
- Set `open` attribute and don't put a value after it.
```
<sy-drawer open></sy-drawer>
```


- Alternatively, you can explicitly set it to true, but to not apply `open`, remove the attribute instead of setting its value to false.
```
<sy-drawer open="true"></sy-drawer>
```

## Example
<Canvas of={DrawerOpenStories.Param} />

<Controls of={DrawerOpenStories.Param} />
