import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as CustomSizeStories from './sy-drawer-customsize.stories';

<Meta of={CustomSizeStories} />

# Custom Size

When `size="custom"`, the `customSize` attribute sets the drawer's pixel dimension along its axis.

<Canvas of={CustomSizeStories.Default} />
<Controls of={CustomSizeStories.Default} />
