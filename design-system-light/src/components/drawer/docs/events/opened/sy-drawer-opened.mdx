import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as OpenedStories from './sy-drawer-opened.stories';

<Meta of={OpenedStories} />

# Opened

The `opened` event fires once the drawer has finished opening.

<Canvas of={OpenedStories.Default} />
