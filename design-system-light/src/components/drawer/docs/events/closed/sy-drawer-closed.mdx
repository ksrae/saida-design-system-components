import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as ClosedStories from './sy-drawer-closed.stories';

<Meta of={ClosedStories} />

# Closed

The `closed` event fires once the drawer has finished closing.

<Canvas of={ClosedStories.Default} />
