import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as DrawerClosedStories from './drawer-closed.stories';

<Meta of={DrawerClosedStories} />

# closed
## Definition
The `closed` event is emitted when the drawer is closed.

## Example
<Canvas of={DrawerClosedStories.Param} />

<Controls of={DrawerClosedStories.Param} />
