import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as TypeStories from './sy-divider-type.stories';

<Meta of={TypeStories} />

# Type

The `type` attribute sets orientation. Values: `horizontal` (default), `vertical`.

<Canvas of={TypeStories.Default} />
<Controls of={TypeStories.Default} />
