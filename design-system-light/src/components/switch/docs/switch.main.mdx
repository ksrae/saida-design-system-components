import { Canvas, Meta, Controls, Story } from "@storybook/blocks";

import * as SwitchParamStory from "./switch.stories";

<Meta of={SwitchParamStory} />

# Switch
## Definition
The Switch, also known as a toggle switch, allows users to toggle between two states, typically "on" and "off" or "enabled" and "disabled."


## Example
<Canvas of={SwitchParamStory.Default} />

<Controls of={SwitchParamStory.Default} />
