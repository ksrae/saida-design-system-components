import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as SwitchLoadingStories from './switch-loading.stories';

<Meta of={SwitchLoadingStories} />

# Loading
## Definition
To set whether the switch is in a loading state. <br/>
When loading is true, disabled attribute is automatically set.<br/>
When loading is false, it follows the user disabled setting. 


## Boolean Attribute Handling
In the source code, <b>loading = ""</b> should be modified as follows
- Set `loading` attribute and don't put a value after it.
```
<sy-switch loading></sy-switch>
```

- Alternatively, you can explicitly set it to true, but to not apply `loading`, remove the attribute instead of setting its value to false.
```
<sy-switch loading="true"></sy-switch>
```


## Example
<Canvas of={SwitchLoadingStories.Param} />

<Controls of={SwitchLoadingStories.Param} />
