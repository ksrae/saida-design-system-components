import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as EventStories from './sy-switch-changed.stories';

<Meta of={EventStories} />

# Changed

Emitted with the new checked value whenever the switch toggles.

<Canvas of={EventStories.Default} />