import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as MondayStartStories from './sy-datepicker-mondaystart.stories';

<Meta of={MondayStartStories} />

# Monday Start

The `mondayStart` attribute starts the week on Monday instead of Sunday.

<Canvas of={MondayStartStories.Default} />
<Controls of={MondayStartStories.Default} />
