import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as DatepickerDisabledStories from './datepicker-disabled.stories';


<Meta of={DatepickerDisabledStories} />

# Disabled
## Definition
The `disabled` attribute allows you to disable the datepicker, preventing user interaction.

## Boolean Attribute Handling
In the source code, disabled = "" should be modified as follows. <br/>
Set the <b>disabled</b> attribute without assigning a value to it. <br/>
```
<sy-datepicker disabled></sy-datepicker>
```
Alternatively, you can explicitly set it to true, but to not apply `disabled`, just remove the attribute instead of setting its value to false.

```
<sy-datepicker disabled="true"></sy-datepicker>
```

## Example
<Canvas of={DatepickerDisabledStories.Param} />

<Controls of={DatepickerDisabledStories.Param} />
