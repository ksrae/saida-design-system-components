import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as PlaceholderStories from './sy-datepicker-placeholder.stories';

<Meta of={PlaceholderStories} />

# Placeholder

The `placeholder` attribute sets the hint text shown when no date is selected.

<Canvas of={PlaceholderStories.Default} />
<Controls of={PlaceholderStories.Default} />
