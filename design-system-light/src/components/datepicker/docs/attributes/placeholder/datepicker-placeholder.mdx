import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as DatepickerPlaceholderStories from './datepicker-placeholder.stories';


<Meta of={DatepickerPlaceholderStories} />

# Placeholder
## Definition
The `placeholder` attribute is used to to set a placeholder text for the datepicker input field. This text is displayed when the input is empty, providing a hint to the user about what to enter.

## Example
<Canvas of={DatepickerPlaceholderStories.Param} />

<Controls of={DatepickerPlaceholderStories.Param} />
