import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as VariantStories from './sy-datepicker-variant.stories';

<Meta of={VariantStories} />

# Variant

The `variant` attribute switches between `date`, `datetime`, `range`, and `time` pickers.

<Canvas of={VariantStories.Default} />
<Controls of={VariantStories.Default} />
