import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as ReadonlyStories from './sy-datepicker-readonly.stories';

<Meta of={ReadonlyStories} />

# Readonly

The `readonly` attribute displays the selected date but blocks edits.

<Canvas of={ReadonlyStories.Default} />
<Controls of={ReadonlyStories.Default} />
