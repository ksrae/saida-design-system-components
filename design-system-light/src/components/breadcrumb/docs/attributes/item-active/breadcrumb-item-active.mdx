import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as BreadcrumbItemActiveStories from './breadcrumb-item-active.stories';


<Meta of={BreadcrumbItemActiveStories} />

# Active Item
## Definition
Sets the active attribute on an item in the itemlist to activate it. <br/>

<br/>

## Boolean Attribute Handling
In the source code, active = "" should be modified as follows
- Set `active` attribute and don't put a value after it.
```
<sy-breadcrumb-item active></sy-breadcrumb-item>
```
- Alternatively, you can explicitly set it  to `true`, but to not apply the `active`, remove the attribute instead of setting its value to`false`.

```
<sy-breadcrumb-item active="true"></sy-breadcrumb-item>
```

## Example
<Canvas of={BreadcrumbItemActiveStories.Param} />

<Controls of={BreadcrumbItemActiveStories.Param} />

