import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as TextareaReadonlyStories from './textarea-readonly.stories';

<Meta of={TextareaReadonlyStories} />

# Readonly
## Definition
Use the `readonly` attribute to set the textarea readonly state.<br/>
It makes the textarea  not mutable, meaning the user can not edit the control.

## Boolean Attribute Handling
In the source code, <b>readonly = ""</b> should be modified as follows
- Set `readonly` attribute and don't put a value after it.
```
<sy-textarea readonly></sy-textarea>
```
- Alternatively, you can explicitly set it to true but to not apply `readonly`, remove the attribute instead of setting its value to false.

```
<sy-textarea readonly="true"></sy-textarea>
```


## Example
<Canvas of={TextareaReadonlyStories.Param} />

<Controls of={TextareaReadonlyStories.Param} />
