import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as EventStories from './sy-tag-selected.stories';

<Meta of={EventStories} />

# Selected

Emitted when the tag is selected.

<Canvas of={EventStories.Default} />