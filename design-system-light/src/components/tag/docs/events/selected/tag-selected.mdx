import { Canvas, Meta, Controls, Story } from '@storybook/blocks';

import * as TagSelectedStories from './tag-selected.stories';

<Meta of={TagSelectedStories} />

# Selected Event
## Definition
The tag has selected event when user clicks a tag.<br/>
The event emits Custom Event.<br/>
Use <b>event.detail.tag</b> to get which includes all properties of tag.<br/>



## Example
<Canvas of={TagSelectedStories.Param} />

<Controls of={TagSelectedStories.Param} />
