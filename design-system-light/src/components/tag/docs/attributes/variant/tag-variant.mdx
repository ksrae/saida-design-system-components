import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as TagVariantStories from './tag-variant.stories';

<Meta of={TagVariantStories} />

# Variant
## Definition
To set the background color of the tag.<br/>
The color variant is : `gray` , `purple` , `blue` , `green` , `cyan` , `yellow` , `orange` , `red`.<br/><br/>
The default value is `gray`.<br/>


## Example
<Canvas of={TagVariantStories.Param} />

<Controls of={TagVariantStories.Param} />
