import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as TagSelectableStories from './tag-selectable.stories';

<Meta of={TagSelectableStories} />

# Selectable
## Definition
To set the tag selectable

## Boolean Attribute Handling
In the source code, <b>selectable = ""</b> should be modified as follows
- Set `selectable` attribute and don't put a value after it.
```
<sy-tag selectable></sy-tag>
```
- Alternatively, you can explicitly set it to true, but to not apply `selectable`, remove the attribute instead of setting its value to false.

```
<sy-tag selectable="true"></sy-tag>
```


## Example
<Canvas of={TagSelectableStories.Param} />

<Controls of={TagSelectableStories.Param} />
