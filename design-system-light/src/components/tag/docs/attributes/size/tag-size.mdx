import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as TagSizeStories from './tag-size.stories';

<Meta of={TagSizeStories} />

# Size
## Definition
The `size` attribute is used to set the size of the tag. <br/>
`small`, `medium`, `large` are the available sizes. <br/>
The default size is `medium`.

## Example
<Canvas of={TagSizeStories.Param} />

<Controls of={TagSizeStories.Param} />
