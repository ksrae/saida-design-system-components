import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as NavDisabledStories from './nav-disabled.stories';

<Meta of={NavDisabledStories} />

# Nav Item Disabled
## Definition
Use the `disabled` attribute to a nav.
If `disabled` attribute is set true, all events will be prevented.<br/>
The `disabled` in parent element(`sy-nav`) will be inherited by all children.

## Boolean Attribute Handling
In the source code, <b>disabled = ""</b> should be modified as follows
- Set `disabled` attribute and don't put a value after it.
```
<sy-nav disabled></sy-nav-item>
```

- Alternatively, you can explicitly set it  to `true`, but to not apply the `disabled`, remove the attribute instead of setting its value to `false`.
```
<sy-nav disabled="true"></sy-nav-item>
```
## Example
<Canvas of={NavDisabledStories.Param} />
<Controls of={NavDisabledStories.Param} />
