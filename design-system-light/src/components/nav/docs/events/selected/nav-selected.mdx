import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as NavSelectedStories from './nav-selected.stories';

<Meta of={NavSelectedStories} />

# Selected
## Definition
Selecting any nav item emits a <b>selected</b> event.<br/><br/>
The `event.detail` includes the value of the nav item.

## Example
<Canvas of={NavSelectedStories.Param} />

<Controls of={NavSelectedStories.Param} />

