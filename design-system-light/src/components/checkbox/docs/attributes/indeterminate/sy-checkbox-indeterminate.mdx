import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as IndeterminateStories from './sy-checkbox-indeterminate.stories';

<Meta of={IndeterminateStories} />

# Indeterminate

The `indeterminate` attribute displays a mixed (dash) state for the checkbox.

<Canvas of={IndeterminateStories.Default} />
<Controls of={IndeterminateStories.Default} />
