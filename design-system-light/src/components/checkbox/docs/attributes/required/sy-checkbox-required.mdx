import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as RequiredStories from './sy-checkbox-required.stories';

<Meta of={RequiredStories} />

# Required

The `required` attribute makes the checkbox required for form submission.

<Canvas of={RequiredStories.Default} />
<Controls of={RequiredStories.Default} />
