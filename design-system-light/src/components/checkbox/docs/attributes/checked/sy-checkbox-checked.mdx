import { Canvas, Meta, Controls } from '@storybook/addon-docs/blocks';
import * as CheckedStories from './sy-checkbox-checked.stories';

<Meta of={CheckedStories} />

# Checked

The `checked` attribute controls the checked state of the checkbox.

<Canvas of={CheckedStories.Default} />
<Controls of={CheckedStories.Default} />
