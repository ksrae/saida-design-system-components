import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as CheckboxDisabledStories from './checkbox-disabled.stories';


<Meta of={CheckboxDisabledStories} />

# Disabled
## Definition
Use the disabled attribute to set the checkbox in a disabled statte, where it cannot be interacted with.

## Boolean Attribute Handling
In the source code, disabled = "" should be modified as follows
- Set `disabled` attribute and don't put a value after it.
```
<sy-checkbox disabled></sy-checkbox>
```
- Alternatively, you can explicitly set it  to `true`, but to not apply the `disabled`, remove the attribute instead of setting its value to`false`.
```
<sy-checkbox disabled="true"></sy-checkbox>
```

## Example
<Canvas of={CheckboxDisabledStories.Param} />

<Controls of={CheckboxDisabledStories.Param} />