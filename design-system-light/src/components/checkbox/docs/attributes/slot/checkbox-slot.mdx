import { Canvas, Meta, Controls, Story } from '@storybook/blocks';
import * as CheckboxSlotStories from './checkbox-slot.stories';

<Meta of={CheckboxSlotStories} />

# Slot
## Definition
The slot allows users to define a custom slot for the checkbox component.<br/>
This slot can be used to insert custom content or elements inside the checkbox, providing flexibility in how the checkbox is rendered.<br/>

## Example
<Canvas of={CheckboxSlotStories.Param} />

<Controls of={CheckboxSlotStories.Param} />